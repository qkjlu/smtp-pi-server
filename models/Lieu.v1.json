{
  "title": "Lieux",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "uuid [clé primaire]"
    },
    "adresse": {
      "type": "string"
    },
    "longitude": {
      "type": "string"
    },
    "latitude": {
      "type": "string"
    },
    "createdAt": {
      "type": "string",
      "description": "timestamp with time zone (généré automatiquement)"
    },
    "updatedAt": {
      "type": "string",
      "description": "timestamp with time zone (généré automatiquement)"
    }
  },
  "required": [
    "id",
    "adresse",
    "longitude",
    "latitude"
  ]
}